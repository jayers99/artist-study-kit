---
title: "Wiki Log"
type: wiki/log
tags: [wiki/log]
---

# Wiki Log

Append-only, chronological record of wiki operations (ingest · query · lint), per the LLM-Wiki method (`/Users/jayers/code/public_shuttle/prompts/llm-wiki.md`). Newest at the bottom. Entry prefix is grep-able: `grep "^## \[" wiki/log.md | tail -5`. Deeper session narrative lives in the `raw/` session handoffs (09, 14, 15, 17).

## [2026-06-19] ingest | Phase 2 wiki stand-up (raw 01–13)
Stood up the synthesis layer: index + 8 stage notes + 6 concept notes from the 11-topic research corpus. Design: [[10-wiki-synthesis-design]]; handoff [[14-phase-2-wiki-session]].

## [2026-06-20] ingest | 16.1 image source hierarchy
Wired [[16.1-image-source-hierarchy]] into [[stage-image-discovery]] — tiered source hierarchy (T1 Wikidata identity layer → T2 Met/AIC/Cleveland high-res+rights → T3 Commons/Europeana → T4 discovery-only), QID as cross-source dedup key.

## [2026-06-20] ingest | UAT + feature-requests + study-dimensions (raw 18, 19, 20)
Batch ingest of three sources added since Phase 2.
- [[18-uat-feedback]] (F6) → **new** [[stage-curation-interview]] (Socratic interview → `study-briefs`); refocused [[stage-curation]] to pure visual rating; F1/F3/F5 hardening folded into [[stage-image-discovery]] open questions; wired [[concept-desirable-difficulty]] + [[concept-worked-example-fading]] to the new stage.
- [[19-stateful-runs-custom-images-staged-analysis]] → stateful/resumable multi-run + custom-image injection added to [[stage-image-discovery]]; narrowing-funnel (≤3–4, progressive zoom) added to [[stage-curation]] / [[stage-visual-analysis]] / [[stage-curation-interview]] as open questions (feature request, not yet decided).
- [[20-dimensions-of-study-cheat-sheet]] → enriched [[concept-formal-analysis-taxonomy]] (18-dimension + core-12 taxonomy, translation-to-study layer) and [[stage-visual-analysis]].
Touched 9 wiki pages + index. Pipeline now 9 stage notes; concepts unchanged at 6.

## [2026-06-20] lint | health check + backlink repair
Clean: no dangling links, no orphans, all `sources:` resolve, every `NN.1` report referenced. Fixed bidirectional `used-by` drift between concepts and stages: dropped stale `stage-curation` from [[concept-worked-example-fading]] (its use moved to [[stage-curation-interview]] in the F6 refocus) and added the missing backlinks ([[stage-style-definition]], [[stage-visual-analysis]]); restored intended concept backlinks in [[stage-works-inventory]] ([[concept-source-trust-signals]]) and [[stage-visual-analysis]] ([[concept-spaced-repetition]]); linked [[concept-formal-analysis-taxonomy]] from [[stage-curation-interview]]. Aligned index numbering (6a/6b) and the worked-example-fading annotation. Two pre-existing drifts predated this session's ingest.

## [2026-06-20] build | raw 19 Thrust 1 shipped — stateful package state
Brainstormed → specced → planned → built (8-task subagent-driven TDD, 215 tests, merged to main) the persistent multi-run backbone from [[19-stateful-runs-custom-images-staged-analysis]] Thrust 1. Spec `docs/superpowers/specs/2026-06-20-stateful-package-state-design.md`; plan `docs/superpowers/plans/2026-06-20-stateful-package-state.md`. `state.json` is now `{artist, completed, runs[], candidates[], sessions[]}`: idempotent `merge_candidates` (QID→inst_ids→work_id key), discovery-run ledger, and repeatable study sessions grouped by dimension; "studied" is a derived ✓ badge, never a selection gate; back-compat `PipelineState` alias + `migrate_legacy`. Recorded the built status in [[stage-image-discovery]] (mergeable discovery + `origin` seam) and [[stage-curation]] (multi-session + studied badge). **Still open from raw 19:** Thrust 2 (custom-image injection — `origin:"user"` seam reserved) and Thrust 3 (narrowing funnel + binary-select gallery), each its own spec.

## [2026-06-20] ingest | 21.1 + 21.2 divergent/convergent thinking
Ingested two NotebookLM reports from the "Divergent/convergent Thinking" notebook (new raw root **21**, no prompt doc — not from the research pipeline): [[21.1-divergent-convergent-strategic-partner-framework]] (a prompting protocol) and [[21.2-llm-creativity-vs-human-cognition-benchmark]] (human-vs-LLM creativity benchmarking). These are **meta** sources (about LLM/human creativity, not art pedagogy), so they contribute a cognitive *frame* the pipeline already instantiates.
- **New** [[concept-divergent-convergent-thinking]] (variation→selection; semantic distance; creative ceiling; human owns the convergent selection). used-by: curation, curation-interview, visual-analysis (clears the rule-of-three promotion bar).
- [[stage-curation]] — narrowing funnel reframed as a divergent→convergent cycle (human converges).
- [[stage-curation-interview]] — grounded the "coverage, not redundancy" rule as *maximizing semantic distance across the study set* (DAT applied to lessons).
- [[stage-visual-analysis]] — drill ideation as a divergent→convergent pass.
- [[concept-desirable-difficulty]] — cross-linked: AI lacks an autonomous selection phase, so handing over the converged answer is the retrieval-strength anti-pattern.
- Index: concept list + raw corpus (new "Creativity / cognition (meta)" line) + source→stage map (2 rows). Touched 6 wiki pages + index; pipeline unchanged at 9 stages; concepts 6 → 7.

## [2026-06-20] build | raw 19 Thrust 2 shipped — custom image injection
Brainstormed → specced → planned → built (10-task subagent-driven TDD, 241 tests, merged to main) the user's-own-images source from [[19-stateful-runs-custom-images-staged-analysis]] Thrust 2. Spec `docs/superpowers/specs/2026-06-20-custom-image-injection-design.md`; plan `docs/superpowers/plans/2026-06-20-custom-image-injection.md`. A re-enterable **import** operation folds a user image folder into `candidates[]` as `origin:"user"` (the seam Thrust 1 reserved). Key decision: **Claude vision → pipeline verify** instead of an external reverse-image API — Claude proposes `{artist,title,date}` per image, the existing `search_wikidata`/`search_aic` board (`make_pipeline_lookup`) corroborates, and `verify_identification` assigns `confirmed`/`proposed`/`off_artist`/`unidentified`. A batch **trust gate** (`import-review.json/.html` → human confirm → `parse_review`) precedes the board (F3 provenance lesson). `ingest_import_review` copies files to `images/user/` and `merge_user_candidate` appends new works or **enriches** an existing board card with the local high-res file (no duplicate); unverified images dedup by source file so re-import is idempotent. Visual analysis reads `local_path` directly. Recorded in [[stage-image-discovery]] (new built custom-image source + `local_path`/`USER`-badge note). **Still open from raw 19:** Thrust 3 (narrowing funnel + binary-select gallery) — the last piece.

## [2026-06-20] build | raw 19 Thrust 3 Spec A shipped — persistent board stars
Brainstormed → split Thrust 3 into **Spec A** (persistent stars + filter/sort) and **Spec B** (progressive-zoom funnel + skip-discovery), then specced → planned → built Spec A (8-task subagent-driven TDD, 261 tests, merged to main) from [[19-stateful-runs-custom-images-staged-analysis]] §(c)/(d). Spec `docs/superpowers/specs/2026-06-20-persistent-board-stars-design.md`; plan `docs/superpowers/plans/2026-06-20-persistent-board-stars.md`. The 1–5★ control returns as a **persistent per-candidate annotation**: `BoardCandidate.stars` survives every session + discovery/import run; the gallery seeds it and exports `stars.json`, which `PackageState.ingest_stars` persists. Collect now **caches thumbnails locally** (`image_download.cache_thumbnails` → `thumbnail_path`), so the board renders offline, never re-pulls, and **sorts by file size** (also year-default / stars) and **filters by stars** (all/unstarred/≥N). The headline change is **stars ⊥ selection**: the old `liked()` (rating ≥ 4) → `selected` coupling is removed; selection is driven by an explicit per-card `selected` flag carried end-to-end through `ingest_selection` / `apply_selection` / `resolve.resolve_selection` / the interview `build_queue(selected_rows(sel), …)`. `liked()` remains only for `migrate_legacy`. New module `scripts/dates.py` (`parse_year`). Recorded in [[stage-curation]] (stateful-stars bullet → BUILT). **Still open from raw 19:** Thrust 3 **Spec B** (progressive-zoom funnel + skip-discovery) and the deferred duplicate-handling-on-re-query spec (`TODO.md`).

## [2026-06-20] build | raw 19 Thrust 3 Spec B shipped — narrowing funnel + skip-discovery
Built Spec B (7-task subagent-driven TDD, 275 tests, merged to main) from [[19-stateful-runs-custom-images-staged-analysis]] §(a)/(b) — **completing Thrust 3, the last open piece of raw 19.** Spec `docs/superpowers/specs/2026-06-20-narrowing-funnel-skip-discovery-design.md`; plan `docs/superpowers/plans/2026-06-20-narrowing-funnel-skip-discovery.md`. The Spec-A board becomes a **two-stage progressive-zoom funnel** on one self-contained page: stage 1 = wide-scan board; **Next** freezes the wide cut and re-renders the same grid zoomed to ~2-wide showing only the selected works at **full size** (`museum_search.display_url` swaps the IIIF size segment to 843px, hotlinked display-only); narrow to **≤4** (`MAX_STUDY`, gated in JS + `parse_study_set` truncation) and **Commit** writes `stars.json` + `selection.json` (frozen wide cut) + new `study-set.json` (narrow cut). The core invariant: the **wide cut is a session record** while **everything expensive runs on the study_set** — `resolve_selection(only=set(study_set))` / `apply_selection(only=…)` bound high-res to the ≤4, and the interview queue is `build_queue(rows)` filtered to study_set. A **skip-discovery study mode** (`PackageState.has_candidates()`) lets one collected board feed many study sessions. New: `study_set_json` path, `parse_study_set`/`load_study_set`, `display_url`, gallery `full_url`. Stars ⊥ selection (Spec A) verified intact in the funnel JS. Recorded in [[stage-curation]] (funnel bullet → BUILT). **raw 19 is now fully built (Thrusts 1–3).** Only the deferred duplicate-handling-on-re-query spec remains (`TODO.md`).

## [2026-06-20] validate | funnel pipeline live e2e (Monet) + Spec-A/B drift repair
Live end-to-end validation of the narrowing-funnel pipeline (Thrust 3, Spec A + B) on real data, after both merged. Committed reusable harnesses (`730aafe`): `e2e/funnel_pipeline.py` (live AIC discovery → `cache_thumbnails` local cache → real funnel `gallery.html` → `display_url` 843px confirmed HTTP 200 live → simulated browser commit → real `ingest_stars`/`ingest_selection`/`load_study_set`/`resolve_selection(only=…)`/`record_session`) and `e2e/commons_resolve.py` (re-resolves the ≤4 study set via live Wikimedia Commons + real `download_candidate`, multi-MB PD files to `images/selected/`). Outside `tests/` so pytest (275, offline) doesn't collect them; `e2e/README.md` + a repo-map line in `CLAUDE.md` added. **Result: 0 skill bugs** — every Spec-B invariant held on real artifacts (study_set ≤ MAX_STUDY(4); study_set ⊆ wide selected; work dropped at zoom stays in `selection.json` but absent from `study-set.json`; resolve + interview queue bounded to study_set; session records both cuts; **stars ⊥ selection** confirmed; `has_candidates()` true). Wikimedia Commons confirmed fully operational. Recorded the live-validated status in the [[stage-curation]] funnel bullet. **Lint fix:** repaired two stale "not yet built / Open" references to Thrust 3 in [[stage-curation]] (the Spec-A bullet and the multi-session bullet) that went stale when Spec B merged — both now point to the BUILT funnel/gallery. Only the deferred duplicate-handling-on-re-query spec remains open (`TODO.md`).

## [2026-06-20] lint | full health check — clean
Ran the lint pass (method ref `llm-wiki.md`) over all 18 wiki notes after the `CLAUDE.md` schema freshen (`951706a`). Mechanical checks via script: **no contradictions, no orphans, no real dangling links**; every stage `sources:` resolves to `raw/`; concept `used-by:` ↔ stage-body link reciprocity intact (all 7 concepts); all 14 `NN.1` reports + feedback/meta sources (18/19/20/21.1/21.2) referenced by ≥1 note. Semantic check: the funnel/stateful/custom-image features show **BUILT** consistently across [[stage-curation]] (L49), [[stage-curation-interview]] (L28 "RESOLVED"), and [[stage-visual-analysis]] (L50–54) — no stale "open question / feature request" language survives from when Thrust 3 was a request. One regex hit (`[[work_id]]` in [[stage-curation-interview]]) confirmed a **false positive**: it sits inside an inline-code span documenting the skill's emitted link format, which Obsidian doesn't parse as a graph edge. **No fixes required.** Only the deferred duplicate-handling-on-re-query spec remains open (`TODO.md`).

## [2026-06-20] validate | full-pipeline Monet master study (live) → digital-friction-lab feature request
Ran the **entire 9-stage skill live on Claude Monet** by driving `skill/SKILL.md` directly (the skill isn't installed at `~/.claude/skills`). End-to-end behaviour, all real network: **Wikidata WDQS was down** (3 ReadTimeouts) → the skill **degraded gracefully** to an AIC-only board (46 PD works, run flagged `degraded=True`, upgradable later) — degraded mode validated for the first time on a real outage. **Firecrawl escalation** carried Stage 2 when museum/scholarly sites bot-blocked WebFetch (403/429): graded 4 live sources (Smarthistory **A**, Met **A**, Wikipedia/National-Gallery **B**). Narrowing funnel → wide cut of 4, study set of 2; the human **converged 3→2 mid-interview** (skip the 3rd) — handled by narrowing `study-set.json`, leaving the dropped work starred + in the wide cut for a later skip-discovery session. Socratic curation interview produced 2 study briefs from the human's own words (anchors: *focal contrast* on a still life, *broken color in the shadow* on a snow-effect Stack); high-res resolved via **AIC IIIF 1686px**; visual analysis grounded in the resolved images; full retention package emitted. **0 skill bugs** on the happy path; the only friction was the external Wikidata outage. **Feature request surfaced and folded into [[stage-study-retention]] (Open questions):** a **medium-aware "digital friction lab"** — a progressive single-variable experiment ladder that guides the learner to *discover* the technical rules themselves (contrast channels that draw the eye; optical-mixing thresholds for "shimmer") **without giving the solution**; folded/escalating hints, master-as-answer-key, leans on digital instruments (Fresco/Photoshop eyedropper, single-variable adjustment layers, blur-as-squint). Deepens [[concept-desirable-difficulty]] + [[concept-divergent-convergent-thinking]]. Reference impl (hand-authored for this run): `studies/claude-monet/learning-path.md`. Two user memories captured (digital medium; friction/self-discovery learning preference). Not yet built — open productizing questions (medium detection/branch, new stage vs `study_retention` enrichment, per-brief vs template-library generation) live in the stage note.

## [2026-06-20] ingest | Monet-run UAT batch (M1–M6) → stage notes
Folded six findings from the live Claude Monet master-study run into the relevant stages' "Open questions / tensions" (new **M-series**, mirroring raw/18's F-series). Severity is the user's own classification.
- **M1 — Socratic interview opening-question sameness** → [[stage-curation-interview]] (*further discussion*). Process is strong, but the first question is identical every run; vary the opening while keeping the observe-first/never-pre-frame contract (seed from cluster/medium, a framing bank, or the user's own observation ritual). Observation is still the right place to start.
- **M2 — image collection is the weakest link** → [[stage-image-discovery]] (*point of concern, MONITOR*). T1/T3 (Wikidata/Commons) usable only briefly across runs; Monet again lost WDQS → AIC-only degraded board. Floated (undecided): an exhaustive/async background collection that accrues into `candidates[]` via the built `merge_candidates` upgrade path; lean harder on T2 museum APIs when T1/T3 are down.
- **M3 — `works.md` canon absent from the board** → [[stage-image-discovery]] (*user-classified FAILURE — most serious finding*). The famous, learnable works in the inventory (Haystacks/Rouen/Gare Saint-Lazare/Water Lilies) never reached the gallery, so the human couldn't curate them. Causes compound: WDQS-down AIC-only board + no reconciliation between Claude's `works.md` canon and what discovery surfaces. Needs a `works.md`↔board reconciliation (per-work targeted lookup for any canon entry with no candidate) + M2's reduced fragility.
- **M4 — user-image injection as canon-gap mitigation** → [[stage-image-discovery]] (*planned next UAT*). Next run will drop user-provided images (built Thrust-2 import) to backfill what discovery missed; watch whether import becomes routine rather than exceptional.
- **M5 — export hand-off friction** → [[stage-curation]] (*low priority*). Funnel Commit downloads the three JSONs to ~/Downloads; the human must hand-move them into `studies/<artist>/`. Options: surface a move command, a local helper endpoint, or watched-folder ingest.
- **M6 — whole-card select, not a checkbox** → [[stage-curation]] (*UX nitpick*). Clicking anywhere on a card should toggle select — except the 1–5★ bar (stars ⊥ selection must hold). Lives in `build_thumbnail_gallery` JS.
All six are **open questions / feature requests**, not yet specced or built. M2+M3 are the substantive ones (image-collection breadth); M1 is a refinement; M4 is a planned test; M5/M6 are UX polish. The [[stage-study-retention]] digital-friction-lab request (logged above) is the seventh open item from the same run.

## [2026-06-20] ingest | candidate image source — Artvee (M2 backlog)
User-suggested image source for the weak collection layer (M2/M3): **Artvee** (https://artvee.com), a curated high-resolution **public-domain** artwork repository — free downloads + a paid membership (the user subscribes) for max-res/bulk. Started a "Backlog — candidate image sources" list in `TODO.md` and cross-referenced it from the [[stage-image-discovery]] M2 bullet. Assessment notes: no public API → fetch via Firecrawl/Scrapy (`raw/01.1`); max-res is login-gated, so it's a **user-credentialed T3 supplement/fallback**, not an anonymous source; rights PD-curated but still verify; dedup by **artist+title** (no QID). Promising for backfilling the M3 `works.md`↔board canon gap when Wikidata/Commons/museum APIs are down or thin. Not yet evaluated.

## [2026-06-21] ingest | 22.1 image duplicate detection (backs deferred re-query dedup)
Ran a NotebookLM deep research (new raw root **22**, prompt `raw/22-image-duplicate-detection-prompt.md`, 64 sources) on the best Python approach to score whether two images depict the **same artwork** (float 0–1, robust to size/quality/crop/color/watermark, tunable threshold) → [[22.1-image-duplicate-detection]]. Direct input for the deferred **duplicate-handling-on-re-query** spec (`TODO.md`): the built `merge_candidates` dedup key is identifier-based (QID → `inst_ids` → `work_id`) and can't catch the same work arriving from two sources with no shared id. Findings: normalize any method to 0–1 (`S = 1 − D/64` hash Hamming; `S = (cos+1)/2` embeddings), threshold ≈ 0.85 = "same artwork" (≥0.95 strict); report's headline pick is **CLIP/CNN via `imagededup`** (Apache-2.0), optimized for rotation/viewpoint robustness, with a hash→embedding fast-filter hybrid. **Wired into [[stage-image-discovery]]** (new Open-questions bullet) with a recorded **tension**: our case is the same painting across museum sources (resolution/crop/JPEG/color-cast, *not rotated*) and identifier-less only in the fallback, so the lighter pure-Python **`imagehash` pHash+wHash** likely suffices as primary — reserve CLIP for genuinely reframed cases. Index: source→stage map + corpus (Domain/tooling) rows added. Not yet specced/built. (Also: logged a `notebooklm-jayers` skill feature request — background/non-blocking resumable research runs — in that skill's new `TODO.md`.)

## [2026-06-21] build | image dedup engine + manifest shipped (Spec A)
Brainstormed → specced → planned → built (4-task subagent-driven TDD, 293 tests, merged to main `b593c8e`) the pure offline perceptual dedup engine — **Spec A** of the deferred duplicate-handling-on-re-query feature (`TODO.md`), grounded in [[22.1-image-duplicate-detection]]. Spec `docs/superpowers/specs/2026-06-21-image-dedup-engine-design.md`; plan `docs/superpowers/plans/2026-06-21-image-dedup-engine.md`. Three modules: `scripts/image_similarity` (pHash+wHash → 0–1 `score = min(both)`, `DUP_THRESHOLD = 0.90`, fail-open `None` on unreadable), `scripts/image_manifest` (`manifest.json` as provenance record **and** dedup index — stored-hash `find_match` skips empty-hash entries, returns max ≥ threshold), `scripts/dedup` (pure `resolve` → keep/merge/add `DedupAction`: winner = largest pixel area → bytes → existing; metadata fills only empty fields; **stars always preserved**; `work_id` stable; no-churn Obsidian-safe canonical rename; **zero filesystem I/O** — Spec B executes the plan). New dep `imagehash` (+ Pillow/numpy/scipy/PyWavelets) — Pillow wasn't previously used. **Reframed during brainstorming** from "user-import review gate" to a *content-deduped library builder*: collection eagerly downloads full-res (user-authorized rights-ungated, recorded-not-enforced), dedups each image against the accumulating library (seeded user images + prior runs), keeps the largest copy, augments/cleans metadata, deletes the loser from the study dir (never the external original), and maintains the manifest. Locked: fully-automatic · largest-dims wins · only-same-full-image (crops stay separate) · 0.90. One review-caught fix (slugify for fallback stems, Obsidian-safety). Recorded **BUILT** in [[stage-image-discovery]]. **Test bed seeded:** `studies/cezanne/images/user/` (30 images incl. real dup pairs — `cezannepotsouptureen.jpg`↔`(1).jpg`, Madame Cézanne JPG↔60MB PSD). **Still open — Spec B (the consumer):** eager download + seed-import + execute `DedupAction` file moves/deletes into `images/library/` + cross-run + Phase-A SKILL wiring + Cezanne e2e. Spec-B seams flagged by final review: move-onto-delete ordering, manifest↔library-dir sync, metadata-merge is existing-wins not QID-authoritative-override.

## [2026-06-21] build | library collection mode shipped (Spec B) — dedup feature complete
Built **Spec B** (8-task subagent-driven TDD, 314 tests, merged to main `3189e2c`) — the consumer of the Spec A engine, **completing the duplicate-handling-on-re-query feature**. Spec `docs/superpowers/specs/2026-06-21-library-collection-mode-design.md`; plan `.../plans/2026-06-21-library-collection-mode.md`. New `scripts/library.py`: `execute_action` (atomic place-winner→delete-loser, never touches externals), `build_library` (batch `resolve`→execute→`upsert`; added/merged_replaced/merged_kept), `make_incoming` (fail-open), `sync_candidates` (manifest→one `BoardCandidate` each, `local_path`/`thumbnail_path`→library file, **stars preserved on re-sync** — the core invariant), `seed_import` (external→`images/user/`→`images/library/`, **move so user/ empties**, originals untouched). `image_download.download_library` + `resolve.default_resolve_url` eager full-res → `images/incoming/`. `paths` gained `library_dir`/`incoming_dir`/`manifest_json`. SKILL Phase-A gained a **library-collection mode that coexists** with the thumbnail board and feeds the existing funnel unchanged (sync makes the deduped library the curation board). Manifest = provenance ledger + dedup index; downloads rights-recorded-not-gated. **Live Cezanne e2e passed** (`e2e/library_collection.py`): Wikidata degraded→AIC-only, **30 seed files → 26 deduped** (`cezannepotsouptureen.jpg`↔`(1).jpg` byte-identical + Madame Cézanne JPG↔60MB PSD collapsed), 34 AIC candidates → 20 downloaded → 16 net-new, 42 final library entries synced. Final-review caught + fixed one Important: `museum_search.display_url` honored `local_path` only for `origin=="user"`, so discovered library cards (which have a local high-res file but empty `thumbnail_url`) showed a broken full-size zoom — now honors `local_path` for any origin (old thumbnail-board cards unaffected). Recorded **BUILT / feature complete** in [[stage-image-discovery]]. **Only remaining dedup follow-on:** QID-authoritative metadata override (existing-non-empty-wins means a seed-first filename title persists even after QID-rich discovery enriches the work) — out of scope, logged for a future spec.

## [2026-06-21] build | QID-authoritative metadata override shipped — dedup follow-on closed
Built the last dedup follow-on (1-task subagent TDD, 318 tests, merged to main `a07405b`). Spec `docs/superpowers/specs/2026-06-21-qid-authoritative-metadata-override-design.md`; plan `.../plans/2026-06-21-qid-authoritative-metadata-override.md`. Surgical change to the shipped Spec A engine: `dedup.resolve`'s metadata merge became **authority-aware** — `if inc.qid and not match.qid:` the incoming's non-empty fields (title/date/qid/inst_ids/rights/medium) override the existing **guess**; `else` keeps existing-non-empty-wins byte-for-byte. So a seed image imported first with a filename-derived title (e.g. `DT4962`) is corrected to the canonical museum/Wikidata title once discovery matches the work. Key invariants: an existing **QID is never clobbered** (incl. by a different incoming QID — keep-existing, conflict visible in `origins`); **metadata authority ⊥ pixel winner** (a smaller authoritative image upgrades the title while the larger seed file stays the kept pixels); **identity stable** (`work_id`/`filename`/`stars` unchanged — no churn). The upgraded title flows to the curation board automatically via `sync_candidates`. No manifest schema change, no Spec B change. Recorded in [[stage-image-discovery]] (carry-forward note resolved). **The duplicate-handling-on-re-query feature is now fully closed** — Spec A (engine) + Spec B (library collection mode) + this override; no open dedup items remain.

## [2026-06-21] validate | live Cézanne run (library mode) → Human Pause 1 + bug found
Ran the full skill on **Paul Cézanne** from the user's own seed collection (`~/icloud/art_slurp/cezanne`, 30 files) through stages 1–5, stopping at **Human Pause 1** (curation gallery). Stages 1–4 authored (`report.md` background+visual-grammar+cheat-sheet, `sources/` 7 graded A–D, `works.md` 6 clusters dual-axis). **Stage 5 library-collection mode** exercised end-to-end on the real package (not a temp dir): `seed_import` **30 → 26 entries** (4 dups collapsed, matches prior e2e; originals untouched, `user/` emptied); live discovery **Wikidata degraded (read timeout — same as prior run), AIC 34 candidates → all 34 downloaded → 30 net-new, 4 collapsed** against seed; `sync_candidates` → **56-card board**, all 56 rendering local images. `gallery.html` built; `image_discovery` marked complete.
**BUG FOUND (real, latent):** `library.execute_action` double-prefixes the package root when `paths.root` is **relative** — `_abs(paths, action.keep_path)` re-joins a root-relative `tmp_path` against `paths.root`, yielding `studies/paul-cezanne/studies/paul-cezanne/images/user/...` → `FileNotFoundError`. The Spec-B e2e never caught it because `scaffold(tempdir,…)` passes an **absolute** root; SKILL.md documents the relative `scaffold('studies', …)` form, so a real skill run hits it. Worked around by addressing the package via an absolute root (`study_paths(str(Path('studies').resolve()), …)`). Fix candidate: normalize `tmp_path`/`keep_path` to root-relative in `make_incoming`/`resolve`, or make `_abs` idempotent. **Logged as open item.**
**Note:** seed filename-titles (e.g. `202339fg`, `1933.423 - The Vase of Tulips`) persisted because Wikidata was degraded — the QID-authoritative override needs a QID-bearing match to fire; a later collect with Wikidata up will canonicalize them (working as designed). Also minor: `library/` has 57 files vs 56 manifest entries (one orphaned file from two adds colliding on the same `work_id` via `upsert`) — cosmetic, non-blocking.

## [2026-06-21] build | fix library relative-root path-doubling bug
Fixed the bug found in the live Cézanne run (above). `library._abs` is now **idempotent**: when `paths.root` is relative, a captured `tmp_path`/`keep_path` that is already root-prefixed (`root in p.parents`) is returned as-is rather than re-joined against `root` (which produced `studies/<slug>/studies/<slug>/images/...` → `FileNotFoundError`). Genuinely root-relative canonical paths (`images/library/y`) still get prefixed; absolute paths still pass through. Root cause: `make_incoming`/`seed_import` capture paths relative to cwd (which include the relative root prefix), while `entry.path` is stored root-relative — `_abs` couldn't distinguish the two. **3 regression tests** added to `tests/test_library.py` (`_abs` idempotency under relative root, full `build_library` + `seed_import` through a relative root via `monkeypatch.chdir`); **321 tests pass** (was 318). One-function change, no schema/API change; the absolute-root path (e2e, tests) is byte-for-byte unaffected. TODO "Bugs" item marked fixed.

## [2026-06-21] ingest | feature request — Apple Photos ingest channel (FB-group curation)
User feature request folded into the backlog. The user curates art by liking/saving images from artist-specific **Facebook groups** into **Apple Photos** (the like = the curation gesture, upstream on FB). Wants the skill to harvest those saved images, identify them, recover their source feed, and mine likes for feed-yield stats. Captured as an **ingest channel** (not a museum source) feeding the BUILT Thrust-2 custom-image injection (identify) + library/dedup mode (fold/quality-winner/manifest), so most machinery already exists. Decomposed into 3 levels in `TODO.md` ("Backlog — feature request: Apple Photos ingest channel"): **L1** Photos→temp adapter (likely `osxphotos`, macOS read-only) → existing identify+dedup (buildable MVP); **L2** provenance recovery (FB strips EXIF → feed from the user's album/keyword *convention*; `origins[]` `source:"apple-photos"`/`source_feed`, `rights:"unknown"`); **L3** like-stats + feed exploration (cheap group-by; crawling FB itself is ToS/auth-gated — separate/flagged). Targets the M2/M3 image-breadth weakness (own likes = high-precision supplement); sibling of the user-credentialed Artvee source. Pointer added to [[stage-image-discovery]] open questions. **Not yet promoted to active work** — brainstorm→spec when picked up; L1 is the MVP.

## [2026-06-21] ingest | feature request — works.md ↔ gallery reconciliation + targeted image backfill
User feature request, folded into the backlog: **promotes the M3 finding** ("works.md canon absent from the board" — the most serious finding of the Monet run) from open question to explicit feature. Guarantee every primary work in `works.md` is present on the gallery board; for missing ones, do **backup web searches / ferret around the internet** for images. Decomposed in `TODO.md` ("Backlog — feature request: works.md ↔ gallery reconciliation + targeted image backfill"): **L1** reconciliation/gap report (match `works.md` ↔ `state.candidates` and list works with no candidate — hard part is **fuzzy title/year matching**, must be **series-aware** (one inventory entry → many candidates, e.g. Mont Sainte-Victoire / 5 Card Players), no silent drops; MVP); **L2** targeted structured backfill (per-work title lookup vs Wikidata/Commons + museum APIs, folded via the BUILT dedup/library pipeline so hits land on the board with provenance/rights); **L3** web-search "ferreting" backfill (general web image search + **Claude-vision verify** per Thrust-2 that the image depicts the named work; web-found = `rights:"unknown"` recorded-not-gated or reference-link-only; truly-missing works get a "could not locate" notice). L2/L3 reuse the BUILT fold + verify — net-new is the L1 matching and the L3 web adapter. Coupled to the M2 breadth work (F1 lighter-QID/`degraded`). Pointer added to the M3 line in [[stage-image-discovery]]. **Not yet active** — brainstorm→spec when picked up; L1 de-risks matching before any fetching.

## [2026-06-21] ingest | feature request (early idea) — subject-matter / human-experience study dimension (essay artifact)
User feature request, folded into the backlog as an **EARLY IDEA** (not yet decomposed). Proposes a **second study dimension** alongside the primary pictorial/visual-language track: for **realism-based / religious / mythological / history / allegorical** art, study *what human experience the work depicts and why* — not only how it's painted. The user affirms the primary purpose stays learning the **visual language**, but the current pipeline **excludes the subject-matter / human-experience dimension**; the artifact of this offshoot is an **essay** (iconographic / narrative / subject-matter research) + imagery, rather than (or in addition to) a drawing study — *Master Studies = essay + imagery*. Noted that the raw material already exists as [[20-dimensions-of-study-cheat-sheet]] dims **9 (Subject Matter) / 14 (Symbol·Meaning·Iconography) / 17 (Intention·Function·Audience)** but was never promoted into a first-class study **track + output**. Deliberately kept under-specified per the user's request: captured the seed framing + a set of **open questions** (where it lives — new stage vs alternate [[stage-visual-analysis]] mode vs [[stage-study-retention]] enrichment; overlap with [[stage-background-research]]; the essay contract; whether it preserves [[concept-desirable-difficulty]]; a possible subject-matter source lane in [[stage-source-grading]]) **to seed a divergent→convergent refinement pass** ([[concept-divergent-convergent-thinking]]) before any spec. Recorded in `TODO.md` ("Backlog — feature request: subject-matter / human-experience study dimension (essay artifact) — EARLY IDEA"); pointer added to [[stage-visual-analysis]] open questions. **Not yet active** — brainstorm with an explicit divergent→convergent pass first, then spec.

## [2026-06-21] ingest | feature request — period / subject-matter scope for a study ("Picasso Blue Period")
User feature request, folded into the backlog. Adds an optional **third study input** beside the existing two (artist name; optional folder of already-collected images): a **scope** narrowing the whole run to a **period or subject matter** of the artist — e.g. *Picasso Blue Period*, *Picasso's mythological works*. Captured the current reality: discovery is a **bulk creator query** (`search_wikidata` + `search_aic`) with **no period/subject filter**, and the Claude-authored stages cover the whole career, so no slice scoping exists today. Decomposed in `TODO.md` ("Backlog — feature request: period / subject-matter scope for a study") into three levels: **L1** accept the scope + scope the cheap **Claude-authored** stages (focus [[stage-background-research]] bio on the period; **filter the canon** in [[stage-works-inventory]] — a generalization of its existing *early-vs-late development tag*) — the buildable MVP, which also forces the **packaging** decision (own `studies/picasso-blue-period/` dir vs a `scope` field under `studies/picasso/`); **L2** scope **structured discovery** (the crux — Wikidata inception `P571` + *depicts* `P180` / *genre* `P136`; AIC date-range + subject keyword), reusing the BUILT merge/dedup/library pipeline; **L3** map a human-friendly scope label (*Blue Period* → ~1901–1904, *mythological* → genre/depicts) to concrete query constraints, likely **Claude-derived per artist** with the mapping recorded under `prompts/`. Pointers added to [[stage-works-inventory]] (canon filter, L1) and [[stage-image-discovery]] (query filter, L2) open questions. Noted it **composes with** the just-logged subject-matter / human-experience study-dimension request (a *subject* scope is simultaneously a scope and a subject-matter study). **Not yet active** — brainstorm → spec when picked up; **L1 is the MVP** (real value before any query work).
